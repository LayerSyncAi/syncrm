# Convex Configuration
# Get your deployment URL from https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Auth (set via `npx @convex-dev/auth` command)
# These are automatically configured when you run the auth setup
# CONVEX_AUTH_PRIVATE_KEY=... (generated automatically)
# CONVEX_AUTH_APPLICATION_ID=... (generated automatically)

# ── Email (Activity Reminders) ─────────────────────────────────────────────────
# Set in Convex dashboard: https://dashboard.convex.dev → Settings → Environment Variables
#
# RESEND_API_KEY   Required to send real emails. Get one at https://resend.com
#                  If absent, emails are logged to the Convex console (dev mode).
# EMAIL_FROM       Optional sender address shown to recipients.
#                  Must match a verified domain in your Resend account.
#                  Defaults to: "SynCRM <noreply@syncrm.app>"
#
# RESEND_API_KEY=re_...
# EMAIL_FROM=SynCRM <noreply@yourdomain.com>
